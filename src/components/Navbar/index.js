import React from "react";

const Navbar = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100px",
        background: "purple",
        color: "white",
        justifyContent: "space-between",
        display: "flex",
      }}>
      <h2>movie star</h2>
      <h2>movie count</h2>
    </div>
  );
};

export default Navbar;
